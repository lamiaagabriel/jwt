<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
      tailwind.config = {
        theme: {
          extend: {
            container: { center: true, padding: '1rem' },
            colors: {
              main: "#da373d",
            },
          },
        },
      }
    </script>
    <title>JWT App</title>
  </head>
  <body class="text-slate-800 font-mono bg-white text-md md:text-lg flex grid grid-rows-[60px_1fr_60px] grid-cols-1 min-h-screen">
   <header class="flex items-center border border-gray-300">
    <nav class="container flex justify-between items-center gap-2">
      <h1><a href="/" class="font-bold text-lg md:text-2xl">JWT Web</a></h1>
      <ul class="flex justify-between items-center gap-2">
        <li><a class="hover:underline hover:text-main py-1 px-3 tranistion-all duration-300" href="/">Home</a></li>
        <% if(author) { %>
        <li><a class="hover:underline hover:text-main py-1 px-3 tranistion-all duration-300" href="/logout">Logout</a></li>
        <% } else { %>
        <li><a class="hover:underline hover:text-main py-1 px-3 tranistion-all duration-300" href="/login">Login</a></li>
        <li><a href="/register" class="bg-main text-white py-1 px-3 rounded border hover:border-main hover:bg-white hover:text-slate-800 tranistion-all duration-300">Register</a></li>
        <% } %>
      </ul>
    </nav>
   </header>

   <main class="flex bg-gray-50 py-6">
      <div class="container flex flex-col justify-center items-center grow">